<div class="container">
    <div class="row">
        <div class="col-sm-12">
            {{#if blogs}}
                {{#each blogs}}
            <a class="btn btn-info">{{title}}</a>
                {{/each}}
            {{else}}
            <p>还没有文章哦~</p>
            {{/if}}
        </div>
    </div>
</div>